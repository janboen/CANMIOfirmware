/* 
 * File:   events1Track.c
 * Author: Jan Boen
 *
 * This class will be used to hold the code for generating & consuming the 1Track specific events and EV's
 * 
 * Created on February 12, 2019, 9:26 AM
 */

//#include <stdio.h>
//#include <stdlib.h>

/*
 * 
 */

/*
int produce1TrackEvent () {

    // NV reading = use NV_SPARE12
    
    // Standard ACON and ACOFF handled by generic CANMIO functionality
    
    // OPCode 8E = normal 3R mode and 8F = 3R mode caused by preset
    // Produce 8E and 8F
    
    // Consume 8E and 8F
    // If incoming 8E then apply local mode change as needed and produce corresponding 8F event
    
    // If incoming 8F then don't do a local preset and send a corresponding 8E as needed
    
    return ();
}

int consume1TrackEvent () {

    // NV reading = use NV_SPARE12
    
    // Standard ACON and ACOFF handled by generic CANMIO functionality
    
    // OPCode 8E = normal 3R mode and 8F = 3R mode caused by preset
    // Produce 8E and 8F
    
    // Consume 8E and 8F
    // If incoming 8E then apply local mode change as needed and produce corresponding 8F event
    
    // If incoming 8F then don't do a local preset and send a corresponding 8E as needed
    
    return ();
}
 */
